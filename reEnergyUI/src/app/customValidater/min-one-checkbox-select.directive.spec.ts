import { MinOneCheckboxSelectDirective } from './min-one-checkbox-select.directive';

describe('MinOneCheckboxSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new MinOneCheckboxSelectDirective();
    expect(directive).toBeTruthy();
  });
});
