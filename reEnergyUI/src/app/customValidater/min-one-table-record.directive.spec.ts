import { MinOneTableRecordDirective } from './min-one-table-record.directive';

describe('MinOneTableRecordDirective', () => {
  it('should create an instance', () => {
    const directive = new MinOneTableRecordDirective();
    expect(directive).toBeTruthy();
  });
});
